## Styleguide

Это сводка простых правил и соглашений, которые мы используем в проекте.

Большая часть из них проверяется и чинится встроенными линтерами (ESLint, Prettier, CSSComb), поэтому здесь они не описываются.


## Содержание
1. [Создание новых компонентов](#Создание-новых-компонентов)
1. [Общие правила](#Общие-правила)
1. [Описание типов](#Типы)
1. [Стили](#Стили)


## Создание новых компонентов
**1.1 Структура компонента**
```
├── Component/
│   ├── SubComponent1/
│   ├── SubComponent2/
│   ├── SubComponent3/
│   │   ├── index.tsx                - корневой компонент SubComponent3
│   │   ├── Background/              - директория компонента Background
│   │   ├── Character/               - директория компонента Character
│   │   │   ├── images/              - директория с изображениями, используемыми в компоненте Character 
│   │   │   ├── index.tsx            - файл с компонентом Character
│   │   │   ├── index.scss           - файл со стилями для компонента Character
│   │   │   ├── store.ts             - файл со стором компонента Character
│   │   ├── images/                  - директория с изображениями, используемыми в компоненте SubComponent3
│   │   ├── store/                   - директория с файлами стора всего упражения, а также сторов, не имеющих view-компонентов
```
📌 Для именования папок компонентов используется **PascalCase**.
Для остальных папок и файлов - **snake_case** или **kebab-case**.

📌 Каждый компонент создаётся в папке, которая содержит 2 основных файла:

- `index.tsx` - из него экспортируется компонент
- `index.module.scss` - основные стили (название блока подтянется из названия папки).

📌 Название директории === названию компонента, экпортируемого из `index.tsx`.

📌 Общие компоненты нужно делать простыми view-компонентами (не привязанными к стору и другим внешним API) и хранить их в `src/ui/components`. 
Каждый такой компонент нужно создавать сразу вместе со stories-файлом для удобства работы.

## Общие правила
**2.1 Деструктуризация объектов**

Не используйте `object.key` в tsx, в методах и событиях,
вместо этого используйте деструктуринг.

>📌 **Исключение**: если обращение к полям объекта происходит не более двух раз

Это правило касается полей **любого объекта**, если он используется более двух раз.

Пример:
```tsx
// 👎❌
render() {
    return (
        <div className={this.props.type}>
            <span>{this.props.value}</span>
            <input type="text" value={this.props.value} />
        </div>
    );
}

// 👍✅
render() {
    const {value, type} = this.props;
    return (
        <div className={type}>
            <span>{value}</span>
            <input type="text" value={value} />
        </div>
    );
}

// 👍✅
render() {
    return (
        <span>{this.props.value}</span>
    );
}
```
**2.2 Не используйте `export default`**

Есть много материалов, почему этого не стоит делать, но из самого очевидного:
- явное лучше неявного;
- код становится однообразнее;
- проще искать использования => проще дебажить => проще создавать переиспользуемый код.

>☝️ Не используйте `export default`, даже когда используете `observer`

**2.3 Не прокидывайте в пропсы компонента целый объект, если вам из него нужны только несколько полей**

В таком случае прокидывайте только конкретные необходимые поля.

```tsx
// 👎❌

render() {
    return (
        <ComponentWithProps {...this.props} />
    );
}

// 👍✅
render() {
    const {prop1, prop2, prop3} = this.props;
    return (
        <ComponentWithProps
            prop1={prop1}
            prop2={prop2}
            prop3={prop3}
        />
    );
}
```

**2.4 Переиспользуйте утилитарные функции.**

📌 В папке `src/utils` лежит много полезных утилит, которые можно и нужно переиспользовать, а не копипастить в свой компонент.


**2.6 Обязательно добавляйте `displayName` анонимным компонентам**

Всегда необходимо указывать displayName для компонентов с `memo()`, `observer()` и т.п.

Это можно сделать разными способами:

```
// 1️⃣ Использовать именованную функцию 

export const component = memo(function Component(props) {})


// 2️⃣ Напрямую прописать displayName

export const Component = memo((props) => {})

Component.type.displayName = 'Component';    // пишем через .type.displayName из-за https://github.com/facebook/react/issues/18026#issue-564002984


// 3️⃣ Ипользовать export as

const Component = (props) => {}

const wrappedComponent = memo(Component);
export {wrappedComponent as Component};
```

Все способы рабочие, можно использовать любой — главное, чтобы в React DevTools было как можно меньше `Аnonymous` компонентов.


**2.7 Именование функций и переменных**

- Функции, возвращающие boolean, и boolean-переменные всегда называем с префиксом `is/has`.

  Пример:
    ```ts
    const hasPremium = true;
    const isLoggedIn = false;
    
    const isFinalTaskFinished = (): boolean => {}
    ```

**2.8 Именование Props**

- любые коллбеки, передаваемые в компонент, всегда называем с префиксом `on`.

  Пример:
    ```tsx
    const handleButtonClick = () => {};
    
    <MyComponent
        isLoaded={isLoaded}
        onButtonClick={handleButtonClick} 
    />
    ```

**2.9 Общие иконки и svg**

🔆Общие иконки и SVG, используемые в разных компонентах, хранятся в `src/ui/icons`.


## Типы

**3.1 Описание Props**
Для любого переиспользуемого компонента создаем type или interface. Имена type и interface должны отражать суть — например, interface для props должен заканчиваться на слово Props (I вначале имени не используем): `interface MyComponentProps`


**3.2 Правила для типов**

1. Стоит избегать таких общих типов как object/function и т.п., в случае, когда возможно конкретизировать — делать это. Например, не стоит описывать тип как просто string, если мы точно знаем, какие именно строки могут быть использованы. Лучше указать:
  ```ts  
    type ButtonProps = {
      size: 'small' | 'medium' | 'large';
    };
  ```
В случае, если по передаваемым значениям не очевидно, что имеется в виду, стоит создавать объект, описывающий возможные значения, так как Direction.Up в примере ниже выглядит понятнее, чем просто значение 1. (enum не используем)
   ```ts  
    const Direction = {
      Up: 1,
      Down: 2,
      Left: 3,
      Right: 4,
    }
  ```
1. В функциях необходимо указывать _типы для параметров и возвращаемый тип_.
1. Описание значения без конкретики недопустимо. Например, нельзя указать тип массив или объект, нужно указать подробнее.
1. Наследование интерфейсов возможно (в разумных случаях — не стоит наследовать у интерфейса с одним единственным значением)
1. При передаче пропсов в компонент следует избегать применения spread-оператора.
   Лучше предавать все параметры явно.
1. Избегать использования опциональных boolean типов. Если очень нужно — то обязательно указать значение по умолчанию.
1. Не используем в проекте `any` | `unknown`

**3.3 @ts-ignore && any**

При добавлении `ts-ignore` или типа `any` нужно обязательно оставлять комментарий, почему это было сделано.

**3.4 Общие типы**

Типы и интерфейсы, используемые более 2-х раз переносим в общий файл в папке `/src/types`

## Стили

Для стилизации компонентов в проекте используется [Sass](https://sass-lang.com).

**4.1 Наименование классов**

При описании классов блоков и элементов используем `camelCase` со следующей схемой наименования:

```scss
.block {}
.block_modifierOne {}
.block__element {}
.block__element_modifierOne_modifierTwo {}
``` 
📌 Имя **корневого блока** подставляется автоматически из css-modules.

📌 Элемент отделяется от блока двойным нижним подчеркиванием, модификатор - одинарным.

**4.2 Не используйте сложные, слишком специфичные селекторы**

При описании имён классов не следует использовать селекторы со сложностью больше двух уровней
т.к. это затрудняет чтение и, как следствие, поддержку кода

```scss
// 👎
.group {
  display: flex;
  justify-content: center;
  pointer-events: none;

  &_clickable > div > div > :nth-child(6) {
    pointer-events: auto;
  }
}

// 👍
.group {
  display: flex;
  justify-content: center;
  pointer-events: none;

  &_clickable  {
    pointer-events: auto;
  }
}
```
Ситуации, когда тебе нужно управлять стилем другого компонента нужно разруливать с помощью передачи модификаторов/className
или, на крайний случай, из tsx.

📌 Негибкие селекторы на дочерние элементы могут быть причиной многих багов и боли :(


**4.3 Не используйте `!important`**

☝️Важно: если вам всё же пришлось прибегнуть к `!important`, необходимо оставить комментарий с пояснением причины.

**♻️4.4 Переиспользуйте общие стили**

В `src/styles` можно найти полезные стили со шрифтами и полезными функциями и утилитами.
